app.py has the main project

paymentProcessor.py has a second flask app that isi a very simplified emulation of a third party payment processor

make sure to run them on different ports if hosting locally
